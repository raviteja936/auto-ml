
{
    "layout": {
        "target": "bandgap_energy_ev",
        "numeric": ["number_of_total_atoms", "percent_atom_al"],
        "categorical": ["spacegroup"]
        },
    "train_path": "./train.csv",
    "test_path": "./test.csv",
    "dense_layers": [[128, "relu"], [128, "relu"], [1]],
    "loss": "MeanSquaredError",
    "optimizer": "adam",
    "metrics": ["accuracy"],
    "learning_rate": 1e-3,
    "batch_size": 32,
    "num_epochs": 2,
    "dropout_rate": 0.3,

    "save_summary_steps": 100
}