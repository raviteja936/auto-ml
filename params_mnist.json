
{
    "layout": {
        "target": "label"
        },
    "train_path": "/home/ravi/.keras/datasets/flower_photos",
    "conv_layers": [[32, 3], [32, 3]],
    "dense_layers": [[128, "relu"], [128, "relu"], [5]],
    "loss": "MeanSquaredError",
    "optimizer": "adam",
    "metrics": ["mse"],
    "learning_rate": 1e-3,
    "batch_size": 32,
    "num_epochs": 1,
    "dropout_rate": 0.3,

    "save_summary_steps": 100
}